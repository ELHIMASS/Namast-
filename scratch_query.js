const { PrismaClient } = require('./src/generated/prisma'); const prisma = new PrismaClient(); async function main() { const clients = await prisma.client.findMany({ where: { OR: [{ nom: { contains: 'Cora', mode: 'insensitive' } }, { prenom: { contains: 'Cora', mode: 'insensitive' } }, { nom: { contains: 'bernillon', mode: 'insensitive' } }, { prenom: { contains: 'bernillon', mode: 'insensitive' } }] } }); console.log(clients); } main().catch(console.error).finally(() => prisma.$disconnect());
